import React from "react";

const Search = ({toSearch}) => {
    return (
        <div className="search-box">
            <input type="text" placeholder={"Search your shit here.."} />
        </div>
    )
};

export default Search;